# interface
radian==0.6.13

# Inline-notebook workflow (molten.nvim client + kernel comms).
# nvim's python3 provider runs from this venv, so pynvim + jupyter_client must
# live in base (ipykernel is installed separately in the Dockerfile runtime
# stage). See docs/plans/inline-images-nvim.md.
pynvim
jupyter_client

# Configuration parsing (for SciAgent-toolkit Codex config)
toml

# Test runner — agents and analysis scripts assume `pytest` is importable/on PATH.
pytest

# .xlsx reader backend for pandas.read_excel (score_eTreg / mt-hi scripts).
# Pinned: an unpinned presence silently vanished on rebuild and broke runs.
openpyxl==3.1.5

# Core Scientific Computing
numpy==1.26.4
scipy>=1.12
pandas==2.2.3
numba
statsmodels
umap-learn
scikit-learn

# Data Storage & Processing
h5py
cooler
tables
htseq
PySam
SWIG
Cython==0.29.32
pybedtools==0.12.0
pyBigWig==0.3.23

# Visualization Libraries
matplotlib
seaborn
plotly
bokeh

# Single-Cell Analysis
anndata==0.10.9
scanpy==1.10.4
scvi-tools==1.2.0
cellrank==2.0.6
scvelo==0.3.2
scrublet==0.2.3
harmony-pytorch
scib==1.1.5
scirpy==0.19.0
muon==0.1.7

# Trajectory & additional tools
monod

# DA/composition and factorization extras
sccoda
cnmf

# Epigenomics
MACS3==3.0.2

deeptools

# Network Analysis
python-louvain==0.16
python-igraph==0.10.4
networkx==3.4.2
leidenalg==0.10.1
pyscenic==0.12.1


# Quality Control & Processing
RSeQC
multiqc==1.25.2
cutadapt

# R and Python Integration (install on-demand to slim image)
# pyreadr==0.5.2
# rpy2==3.5.17
# anndata2ri==1.3.2

# Differential expression / activity inference (Python)
decoupler
pydeseq2
