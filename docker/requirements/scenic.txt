# SCENIC+ stack — ISOLATED env (usepy scenic), not layered on base.
# pycisTopic pins pandas==1.5 against base's 2.2.3, so inheriting base would
# shadow pandas for scanpy/anndata/muon. Nothing from base is visible here,
# hence the full stack below. Neither tool is on PyPI; both install from git.

# --- hard pins the SCENIC+ tools impose -------------------------------------
numpy==1.26.4
pandas==1.5.3

# --- analysis stack (not inherited, see header) -----------------------------
scipy>=1.12,<1.15
scanpy==1.9.8
anndata==0.10.9
matplotlib
seaborn
pyarrow
polars
h5py
tables
scikit-learn
statsmodels
tqdm
ipykernel

# --- SCENIC+ components -----------------------------------------------------
# LDA topic modelling; its Mallet backend uses /opt/mallet (mallet on PATH).
pycistopic @ git+https://github.com/aertslab/pycisTopic.git@787ce422a37f5975b0ebb9e7b19eeaed44847501

# Motif enrichment; point at $REFCACHE_ROOT/cistarget/current (refdata/README.md).
pycistarget @ git+https://github.com/aertslab/pycistarget.git@5aa517604e4842539a7531c16905825dc7cb80fb

# scenicplus is not pinned here: its constraints are tight enough to break the
# whole resolve. Install on top when needed:
#   usepy scenic && pip install 'scenicplus @ git+https://github.com/aertslab/scenicplus.git'
