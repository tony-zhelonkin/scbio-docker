# Communication inference ecosystem (py3.10 compatible where possible)
liana[common]
cellphonedb
# scGLUE (ensure TF2 installed if required wheels exist)
scglue

# SCENIC+ stack has MOVED to its own isolated env — see scenic.txt / `usepy scenic`.
# It cannot live here: pycisTopic pins pandas==1.5 while liana/cellphonedb are on
# pandas 2.x, so co-installing them makes this env unresolvable.
